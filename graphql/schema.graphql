interface Node {
  id: ID!
}

type Query {
  viewer: User
  themes(filter: String = "related"): [User]
  node(id: ID!): Node
}

type User implements Node {
  id: ID!
  name: String
  isActive: Boolean
}
